import { HomeContainer, HomeHeader, HomeTitle, HomeBody, HomeSection } from "./home";
import styled from "styled-components";

export const UsuarioContainer = styled(HomeContainer)`
`;

export const UsuarioHeader = styled(HomeHeader)`
 
`;

export const UsuarioTitle = styled(HomeTitle)`
    margin-left: 1em;
`;

export const UsuarioBody = styled(HomeBody)`

`

export const UsuarioSection = styled(HomeSection)`
`