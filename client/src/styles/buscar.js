import { HomeContainer, HomeHeader, HomeTitle, HomeBody, HomeSection } from "./home";
import styled from "styled-components";

export const BuscarContainer = styled(HomeContainer)`
`;

export const BuscarHeader = styled(HomeHeader)`
 
`;

export const BuscarTitle = styled(HomeTitle)`
    margin-left: 1em;
`;

export const BuscarBody = styled(HomeBody)`

`

export const BuscarSection = styled(HomeSection)`
`