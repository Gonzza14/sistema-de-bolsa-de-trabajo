import { HomeContainer, HomeHeader, HomeTitle, HomeBody, HomeSection } from "./home";
import styled from "styled-components";

export const LoginContainer = styled(HomeContainer)`
`;

export const LoginHeader = styled(HomeHeader)`
 
`;

export const LoginTitle = styled(HomeTitle)`
    margin-left: 1em;
`;

export const LoginBody = styled(HomeBody)`

`

export const LoginSection = styled(HomeSection)`
`