import { HomeContainer, HomeHeader, HomeTitle, HomeBody, HomeSection } from "./home";
import styled from "styled-components";

export const PostulacionesContainer = styled(HomeContainer)`
`;

export const PostulacionesHeader = styled(HomeHeader)`
 
`;

export const PostulacionesTitle = styled(HomeTitle)`
    margin-left: 1em;
`;

export const PostulacionesBody = styled(HomeBody)`

`

export const PostulacionesSection = styled(HomeSection)`
`