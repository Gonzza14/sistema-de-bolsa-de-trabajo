import { DataContainer } from "../base";
import styled from "styled-components";

export const PostulacionesSection = styled(DataContainer)`
  flex-direction: row;
  flex-wrap: wrap;
  width: 100%;
  justify-content: center;
`;
