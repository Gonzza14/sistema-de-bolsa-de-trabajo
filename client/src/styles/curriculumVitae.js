import { HomeContainer, HomeHeader, HomeTitle, HomeBody, HomeSection } from "./home";
import styled from "styled-components";

export const CVContainer = styled(HomeContainer)`
`;

export const CVHeader = styled(HomeHeader)`
 
`;

export const CVTitle = styled(HomeTitle)`
    margin-left: 1em;
`;

export const CVBody = styled(HomeBody)`

`

export const CVSection = styled(HomeSection)`
`